services:
  sql:
    image: mcr.microsoft.com/mssql/server:2022-latest
    platform: linux/amd64
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "${MSSQL_SA_PASSWORD:-Your_password123}"
    ports:
      - "1433:1433"
    volumes:
      - cms-sql-data:/var/opt/mssql

  api:
    build:
      context: .
      dockerfile: src/Cms.Api/Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__DefaultConnection: "Server=sql,1433;Database=CmsDb;User Id=sa;Password=${MSSQL_SA_PASSWORD:-Your_password123};TrustServerCertificate=True"
      Jwt__Key: "${JWT_KEY:-CmsDevelopmentOnlyKey_Replace_InProduction_32!}"
      Database__ApplyMigrationsOnStartup: "true"
    depends_on:
      - sql
    ports:
      - "5101:8080"
    volumes:
      - cms-uploads:/shared/uploads

  admin:
    build:
      context: .
      dockerfile: src/Cms.Admin/Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__DefaultConnection: "Server=sql,1433;Database=CmsDb;User Id=sa;Password=${MSSQL_SA_PASSWORD:-Your_password123};TrustServerCertificate=True"
      Database__ApplyMigrationsOnStartup: "true"
    depends_on:
      - sql
    ports:
      - "5201:8080"
    volumes:
      - cms-uploads:/shared/uploads

volumes:
  cms-sql-data:
  cms-uploads:
